export enum SocketSubscribeEventEnum {
  JOIN_ROOM = 'JOIN_ROOM',
  LEAVE_ROOM = 'LEAVE_ROOM',
}

export enum SocketEmitEventEnum {
  ONLINE_USERS = 'ONLINE_USERS',
  JOIN_ROOM_INVITE = 'JOIN_ROOM_INVITE',
  WINNER_DETERMINED = 'WINNER_DETERMINED',
}
